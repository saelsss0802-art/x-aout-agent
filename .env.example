# Supabase project URL (Frontend / Backend both read-only access use cases)
SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key (can be used by frontend)
SUPABASE_ANON_KEY=your-anon-key

# Server-only key. NEVER expose this to the frontend/browser.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Postgres connection string used by SQLAlchemy/Alembic
# Obtain from Supabase Dashboard -> Connect -> Connection string
# If IPv4-only network, use the pooler / IPv4-compatible host from Connect tab.
DATABASE_URL=postgresql+psycopg://postgres.your-project-ref:your-db-password@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres

API_HOST=0.0.0.0
API_PORT=8000
