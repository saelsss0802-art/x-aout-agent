# Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Public anon key (can be used by frontend)
SUPABASE_ANON_KEY=your-anon-key

# Server-only key (backend only). NEVER expose this to frontend/browser.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase Postgres connection string used by SQLAlchemy/Alembic (required; no localhost fallback)
# Recommended SQLAlchemy format:
#   postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
# Obtain host/port/db/password from Supabase Dashboard -> Connect.
# If IPv6 is unstable on your network, use the pooler (IPv4-compatible) host in Connect.
DATABASE_URL=postgresql+psycopg://postgres.your-project-ref:your-db-password@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres

API_HOST=0.0.0.0
API_PORT=8000

USE_GEMINI_WEB_SEARCH=0
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_GROUNDING_UNIT_COST=1.00
